סיכום השיחה — ריוויו קוד לפרקטיקום (06.07.2026)
================================================

מה עשינו היום, בקצרה:
עברנו קובץ-קובץ על כל מה שנכתב ב-develop על ידי שלוש התלמידות (אסתי, תמר, מלכי),
מצאנו ותיקנו 9 באגים (חלקם אבטחה, חלקם פונקציונליים), תיעדנו הכל, וביצענו commit.

הבראנץ' שעבדנו עליו: fix/practicum-review (נוצר מ-develop).
העבודה הקודמת שלך על refactor/filter-pipeline-workflows נשמרה בצד (git stash) ולא נגענו בה.

--------------------------------------------------------
מה תוקן היום (9 פריטים):
--------------------------------------------------------

1. IDOR בתגובה לפנייה (מלכי) — contactMessageController.ts
   כל משתמש מחובר יכל להשיב לפנייה של משתמש אחר ולראות את כל ההיסטוריה שלה.
   תוקן: נוספה בדיקת בעלות זהה לשתי הפונקציות השכנות באותו קובץ.

2. אובדן שקט של פנייה בכשל מייל (מלכי) — contactController.ts
   אם שליחת מייל ההתראה נכשלה, המשתמש קיבל שגיאה גם שהפנייה כבר נשמרה בהצלחה.
   תוקן: שליחת המייל הפכה ל-best-effort (כשל בה לא מפיל את הבקשה).

3+4. שאילתת AI לא מסוננת + באג double-query (תמר) — tenderBoardAIService.ts / tenderBoardService.ts
   בחיפוש החכם, ה-AI היה מייצר שאילתת Mongo גולמית ללא הגבלה על שדות/אופרטורים —
   סיכון ל-prompt injection. בנוסף התגלה שהפונקציה שיצרה את השאילתה גם הריצה אותה
   בעצמה, וה-caller הריץ אותה שוב על התוצאה (מערך תוצאות, לא שאילתה) — כך שהחיפוש
   החכם תמיד התעלם בפועל מהסינון של ה-AI. תוקן: הפרדת "בניית שאילתה" מ"הרצתה",
   והוספת allowlist של שדות/אופרטורים מותרים.

5. חוסר בדיקת בעלות במכרזים (תמר) — tenderBoardController.ts
   כל משתמש מחובר יכל לערוך/לסגור/למחוק מכרז של כל משתמש אחר, ו-publisherUserCode
   הגיע מה-client (localStorage) בלי אימות. תוקן: publisherUserCode נגזר מהמשתמש
   המאומת בשרת, ונוספה בדיקת isOwnerOrAdmin לפני עדכון/סגירה/מחיקה. נוספו גם 7
   בדיקות (טסטים) חדשות שמוודאות זאת.

6. race condition בארנק הארגון (אסתי) — organizationService.ts / organizationRepository.ts
   טעינת ארנק נעשתה בקריאה-חישוב-כתיבה (לא אטומי) — שתי טעינות בו-זמנית יכלו
   לאבד אחת מהן בלי שגיאה. תוקן: שימוש באופרטור $inc האטומי של MongoDB.

7. route מת שעקף אימות מעברי-סטטוס (אסתי) — organizationRouter.ts
   נמצא endpoint (PATCH /organizations/pending/:id) שהיה יכול לעקוף לגמרי את כל
   בדיקות המעבר-מצב שכבר נבנו ב-approve/reject. בדקנו את ה-client ואישרנו שאין
   שום קריאה אליו בפועל — הוסר לגמרי.

8. מיפוי שגוי בתגובת ה-AI ב"יצירת מכרז חכמה" (תמר) — CreateTender.tsx
   הקוד חיפש response.product אבל השרת מחזיר response.tender — כך שהתכונה
   הציגה הודעת הצלחה אבל אף פעם לא באמת מילאה את הטופס. תוקן.

9. חוסר mock ל-logger בטסטים (תמר ומלכי) — tenderBoardService.test.ts / newsService.test.ts
   טסטים ניסו בפועל לכתוב ל-MongoDB לא זמין וקפאו ~10 שניות לכל קריאת לוג.
   תוקן: מוקאו את ה-logger, בדומה למה שכבר נעשה בקובץ אחד מהשניים.

--------------------------------------------------------
מה תועד בכוונה בלי לתקן (דורש החלטת צוות):
--------------------------------------------------------

A. race condition במגבלת משתמשים לארגון (אסתי) — דורש שינוי סכימה (מונה persistent)
   או טרנזקציות Mongo. לא תוקן כדי לא לשנות סכימה בלי הצוות נוכח.

B. קוד כפול של בדיקת הרשאות ב-8 handlers ב-organizationController.ts (אסתי) —
   עובד נכון היום, אבל מומלץ refactor ל-middleware משותף בעתיד.

--------------------------------------------------------
קבצים שנוצרו/עודכנו:
--------------------------------------------------------

- docs/practicum-review.md — הסקירה הראשונית: מי עשתה מה, מה מוזג ומה לא,
  ובסוף פרק "Per-student summary" עם הערכה איכותית לכל תלמידה (מה חזק, מה
  לתקן, דוגמאות ללימוד).
- docs/practicum-session-log.md — יומן מפורט של כל תיקון בפועל: הבאג, ההשפעה,
  התיקון, ולקח כללי ("reusable lesson") לכל אחד — מיועד להעלאה לאייג'נט
  הארגוני ב-Claude.ai שהקמת למפתחות.

--------------------------------------------------------
מצב הבראנץ'ים:
--------------------------------------------------------

- fix/practicum-review — כל התיקונים היום, עם commit אחד.
- develop — לא שונה (העבודה בוצעה על בראנץ' נפרד).
- refactor/filter-pipeline-workflows — העבודה הקודמת שלך נשמרה ב-stash,
  לא נגעתי בה. (git stash list כדי לראות, git stash pop כדי להחזיר).

--------------------------------------------------------
מה עוד פתוח (לא נגענו בו היום):
--------------------------------------------------------

- הבראנץ' של מרגלית (פורום) — מעולם לא מוזג, נוגע באותם קבצי ארגונים
  שאסתי וגרינש עדיין עובדות עליהם. הסיכון הכי גדול לאובדן עבודה.
- שאר הבראנצ'ים שלא מוזגו: payment-payme, articles-and-ui,
  organization-approval-workflow / organization-status-fix, agents-downloads,
  language-toggle.
- 4 commits של revert על main שלא קיימים ב-develop — develop עדיין מכיל
  פיצ'רים שבוטלו במפורש ב-main, שווה בירור למה.
